# 個資／機密掃描的允許清單
#
# 每行一條正規表示式，比對到「命中內容」或「該行內容」就跳過。
# **每一條都必須寫明為什麼安全**——沒有理由的例外，久了就會變成
# 「把所有警告關掉」的地方，掃描也就失去意義。

# --- SNMP OID 與 MIB 前置碼 -------------------------------------------------
# OID 的點分數字格式與 IPv4 完全相同（`1.3.6.1.2.1` 前四段是合法 IPv4）。
# 程式已用 _looks_like_real_ip() 過濾大部分，這裡處理漏網的。
^\s*#?\s*\.?1\.3\.6\.1
\bOID\b
\bsysObjectID\b
entPhysical|entPhySensor|hrStorage|ifXTable|nsExtend

# --- 通配與迴路位址 -------------------------------------------------------
# 0.0.0.0 是綁定位址、127.0.0.1 是健康檢查目標，都不是現場資訊。
0\.0\.0\.0
127\.0\.0\.1
::1\b

# --- 文件用保留範圍（RFC 5737 / RFC 3849 / RFC 7042）----------------------
192\.0\.2\.|198\.51\.100\.|203\.0\.113\.
2001:db8
00:00:5[eE]:00:53

# --- 授權條文 -------------------------------------------------------------
# GPL 全文含 FSF 的聯絡信箱與地址，那是授權文件的一部分，必須保留原樣。
^\s*(Free Software Foundation|licensing@fsf\.org|<https://www\.gnu\.org)

# --- 專案本身的公開聯絡方式 -----------------------------------------------
# 作者在公開 repo 上刻意公開的聯絡方式，不是洩漏。
jasoncheng7115
github\.com/jasoncheng

# --- 測試中的合成位址 -----------------------------------------------------
# 這些檔案裡的 IP 是**測試夾具**，不是對真實主機的參照：
#   test_preauth_gate.py  來源 ACL 的比對案例（允許/拒絕的網段與位址）
#   test_route_dedup.py   多播 224.0.0.0、廣播 255.255.255.255、預設路由 0.0.0.0
# 它們必須是具體位址才測得出網段比對邏輯，換成文件用範圍不會讓測試更安全，
# 只會讓測試意圖變模糊。
^\s*(_r\(|assert |def test_|\s+["']?(?:192\.168|10\.[0-9]|172\.(?:1[6-9]|2[0-9]|3[01]))).*#?.*$
224\.0\.0\.0|255\.255\.255\.(?:0|255)|239\.255\.255\.250

# --- 程式碼中的網段計算常數 -----------------------------------------------
# 子網路遮罩與預設閘道的判定值，不是現場位址。
0\.0\.0\.0/0|/(?:8|16|24|32)\b

# --- 專案的公開作者資訊 ---------------------------------------------------
# pyproject.toml 的作者信箱是套件中繼資料，本來就要公開。
^\s*(authors|maintainers)\s*=|"email"\s*:

# --- 前置閘門測試的網段案例 -----------------------------------------------
# test_preauth_gate.py 全檔都是來源 ACL 的比對案例，需要具體位址才測得出
# 網段包含關係。換成文件用範圍不會更安全，只會讓測試意圖變模糊。
^\s*(#|""")?.*\b(allowed|denied|net|cidr|source|src|addr)\b.*(?:192\.168|10\.|172\.)

# RTF control words in packaging/make-ui-assets.py look like UNC paths to the
# unc-path rule: `\\par` is an escaped backslash followed by a keyword, not a
# server name. The generator writes the licence page's RTF, so these literals
# have to stay.
out\.append\("\\\\par
